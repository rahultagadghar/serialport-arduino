export interface serialPortList {
  manufacturer: string;
  serialNumber: string;
  locationId: string;
  vendorId: string;
  productId: string;
  comName: string;
}

export interface questions {
  PORT_NUMBER: string;
}
